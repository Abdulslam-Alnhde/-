{
  "name": "university-exam-system-monorepo",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently -n web,api -c blue,magenta \"powershell -NoProfile -Command \\\"Set-Location frontend; npx --no-install next dev\\\"\" \"powershell -NoProfile -Command \\\"Set-Location backend; npm run dev\\\"\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "db:generate": "npm --prefix backend run db:generate",
    "db:push": "npm --prefix backend run db:push",
    "db:seed": "npm --prefix backend run db:seed",
    "db:seed:force": "npm --prefix backend run db:seed:force",
    "lint": "npm --prefix frontend run lint",
    "verify-ai": "npm --prefix backend run verify-ai"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
