# Monorepo: use split env files for local dev:
# - frontend/.env — see frontend/.env.example (NextAuth + INTERNAL_API_* + DATABASE_URL for login)
# - backend/.env — see backend/.env.example (DATABASE_URL, AI keys, INTERNAL_API_SECRET, PORT)
